name: sentiment-tuning-project

docker_env:
  image: python:3.10-slim

entry_points:
  main:
    parameters:
      csv_path: path
    command: >
      python modelling_tuning.py --csv_path {csv_path}
